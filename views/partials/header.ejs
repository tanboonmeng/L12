<!DOCTYPE html>
<html lang="en">
<head>
    <%# Below code makes sure special characters show up correctly %>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>My Expense Tracker</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">
    <%# Below provides navigation links across the app with solid blue background %>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
        <div class="container">
            
            <a class="navbar-brand d-flex align-items-center fw-bold text-white" href="/">
                <img src="/image/calculator.png" 
                     alt="Calculator Logo" 
                     style="width: 30px; height: auto; margin-right: 8px; filter: brightness(0) invert(1);">
                ExpenseTracker
            </a>
  
            <%# Below code pushes menu links to the far right side %>
            <div class="navbar-nav ms-auto">
                <%# Below codes provide hyperlink for pages formatted with crisp white navigation text %>
                <a class="nav-link text-white fw-medium me-2" href="/">Home</a>
                <a class="nav-link text-white fw-medium" href="/add">Add Expense</a>
            </div>
        </div>
    </nav>